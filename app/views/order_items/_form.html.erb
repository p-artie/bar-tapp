<%= form_with(model: order_item, local: true) do |form| %>
  <% if order_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_item.errors.count, "error") %> prohibited this order_item from being saved:</h2>

      <ul>
      <% order_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :shots %>
    <%= form.number_field :shots, id: :order_item_shots%>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity, id: :order_item_quantity %>
  </div>

  <div class="field">
    <%= form.label :instructions %>
    <%= form.text_area :instructions, id: :order_item_instructions %>
  </div>

  <div class="field">
    <%= form.label :drink_id %>
    <%= form.text_field :drink_id, id: :order_item_drink_id %>
  </div>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id, id: :order_item_order_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
